{
  "name": "codematrix",
  "version": "1.0.0",
  "description": "Static scan for AI-generated technical debt — hallucinated (phantom) npm dependencies and dead-code candidates. Runs locally, no account needed.",
  "keywords": [
    "cli",
    "static-analysis",
    "dependencies",
    "npm-audit",
    "dead-code",
    "ai",
    "slopsquatting",
    "supply-chain-security"
  ],
  "author": "CodeAudit",
  "license": "MIT",
  "homepage": "https://github.com/DelTa-0/codeaudit",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DelTa-0/codeaudit.git",
    "directory": "cli"
  },
  "bugs": "https://github.com/DelTa-0/codeaudit/issues",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "codematrix": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && node build.mjs",
    "test:llm-config": "tsx test/llm-config.ts",
    "test:byok-redaction": "tsx test/byok-redaction.ts",
    "prepublishOnly": "npm run build && npm run test:ground-truth --prefix ../server && npm run test:ground-truth-python --prefix ../server && npm run test:llm-config && npm run test:byok-redaction && npm run test:llm-protocol --prefix ../server"
  },
  "devDependencies": {
    "@codeaudit/engine": "*",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
