ANSWER = 42


def used_helper(value):
    return value.strip()


def calculate_legacy_discount(amount):
    # references a feature flag that no longer exists
    return amount * 0.9


def zombie_formatter(value):
    return str(value).strip()
