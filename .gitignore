node_modules/
dist/
.env
*.local
.DS_Store
*.log
.tmp/
coverage/

# Personal defense/presentation materials — never shipped, never reviewed
/defense/

# Obsidian workspace state — per-machine, changes on every open
.obsidian/

# Claude Code personal overrides (project config in .claude/ is committed)
.claude/settings.local.json

# Deployment topology and runbook — kept local. Nothing secret in here
# (values are placeholders), but the production compose file lives here and
# it is not something a reader of this repo needs.
/deploy/
